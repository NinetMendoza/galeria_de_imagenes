import { Routes } from '@angular/router';
import { SavedViewComponent } from './pages/saved-view/saved-view.component';

export const SAVED_ROUTES: Routes = [
  {
    path: '',
    component: SavedViewComponent,
  },
];
